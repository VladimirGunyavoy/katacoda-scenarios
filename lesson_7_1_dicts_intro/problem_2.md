Создайте словарь «покупка» (purchase) c ключами:

-   `'unit_price'` — цена за единицу товара, равная 5000;
-   `'quantity'` — количество товара, равное 3;
-   `'total_cost'` — общая стоимость, равная `unit_price * quantity`.